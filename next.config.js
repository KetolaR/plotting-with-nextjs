/** @type {import('next').NextConfig} */
const nextConfig = {
  // If you later serve this behind a reverse‐proxy or want to customize base paths, do it here
  reactStrictMode: true
};

module.exports = nextConfig;
